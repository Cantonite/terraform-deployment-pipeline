name: Continuous Deployment Pipeline

on:
  push:

jobs:
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environments:
          - development
          - staging
          - production
    defaults:
      run:
        working-directory: ./terraform/template
    steps:
      - uses: actions/checkout@v2
      - uses: hashicorp/setup-terraform@v1
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
      - name: Terraform fmt
        id: fmt
        run: terraform fmt -check
        continue-on-error: true
      - name: Terraform Init
        id: init
        run: terraform init -backend-config="workspaces.name=terraform-deployment-pipeline-development"
      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color

  # deploy-development:
  #   name: Deploy Development
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./terraform/environments/development
  #   needs:
  #     - ci
  #   environment: 
  #     name: development
  #     url: https://app.terraform.io/app/Cantonite/workspaces/terraform-deployment-pipeline-development
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: hashicorp/setup-terraform@v1
  #       with:
  #         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
  #     - name: Terraform Init
  #       id: init
  #       run: terraform init
  #     - name: Terraform Plan
  #       id: plan
  #       run: terraform plan
  #     - name: Terraform Apply
  #       id: apply
  #       run: terraform apply -auto-approve

  # deploy-staging:
  #   name: Deploy Staging
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./terraform/environments/staging
  #   needs:
  #     - deploy-development
  #   environment: 
  #     name: staging
  #     url: https://app.terraform.io/app/Cantonite/workspaces/terraform-deployment-pipeline-development
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: hashicorp/setup-terraform@v1
  #       with:
  #         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
  #     - name: Terraform Init
  #       id: init
  #       run: terraform init
  #     - name: Terraform Plan
  #       id: plan
  #       run: terraform plan
  #     - name: Terraform Apply
  #       id: apply
  #       run: terraform apply -auto-approve

  # deploy-production:
  #   name: Deploy Production
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./terraform/environments/production
  #   needs:
  #     - deploy-staging
  #   environment: 
  #     name: production
  #     url: https://app.terraform.io/app/Cantonite/workspaces/terraform-deployment-pipeline-development
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: hashicorp/setup-terraform@v1
  #       with:
  #         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}
  #     - name: Terraform Init
  #       id: init
  #       run: terraform init
  #     - name: Terraform Plan
  #       id: plan
  #       run: terraform plan
  #     - name: Terraform Apply
  #       id: apply
  #       run: terraform apply -auto-approve